# Carregado automaticamente pelo `vite build` (mode production).
# Sem isso o bundle sai com "undefined/rpc" e o painel quebra em produção.
# Em dev, o .env local (gitignored) continua valendo.
VITE_API_URL=https://api.gasolina.cloud
